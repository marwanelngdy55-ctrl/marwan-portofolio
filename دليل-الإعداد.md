# دليل تشغيل لوحة التحكم والمدونة

## 1) شغّل قاعدة البيانات
1. افتح مشروعك على supabase.com
2. من القائمة الجانبية: **SQL Editor** → **New query**
3. انسخ محتوى ملف `supabase-schema.sql` بالكامل، الصقه، واضغط **Run**
4. لو ظهر "Success" يبقى تمام — الجداول والصلاحيات اتعملت.

> **لو عندك مشروع شغال من قبل**: الملف آمن تشغّله تاني براحتك — بيضيف بس اللي ناقص (عمود Alt text وTitle لصورة الغلاف، باكت تخزين صور الموقع `site-images`، عمود "نشط/موقوف" للأعضاء، اسم الكاتب وإعدادات الظهور للمقالات، ونظام الصلاحيات الجديد، وميزة جدولة النشر) من غير ما يمسح أي بيانات موجودة. لازم تشغّله تاني عشان تفعّل ميزة الصلاحيات والفصل بين مقالات الأعضاء **وميزة جدولة النشر التلقائي**.

## 2) فعّل تسجيل الدخول بالإيميل
- من القائمة الجانبية: **Authentication → Providers** → تأكد إن **Email** مفعّل (مفعّل افتراضيًا عادة).
- من **Authentication → URL Configuration**: حط رابط موقعك (بعد ما ترفعه على GitHub Pages مثلاً) في **Site URL**، عشان روابط الدعوة تشتغل صح.

## 3) اعمل أول حساب لنفسك (Owner)
1. من **Authentication → Users → Add user** → أدخل إيميلك وكلمة مرور.
2. روح لـ **SQL Editor** وشغّل الكود ده (غيّر الإيميل بتاعك):
```sql
update public.profiles set role = 'owner' where email = 'ايميلك@هنا.com';
```
دلوقتي تقدر تدخل على `dashboard.html` بنفس الإيميل وكلمة المرور دي.

## 4) نظام الصلاحيات الجديد (مدير / عضو)

- **مدير (owner)**: هو بس اللي يقدر يفتح تبويب "الفريق"، يضيف أعضاء جدد، ويحدد لكل عضو صلاحيته (مدير أو عضو)، وكمان يقدر يوقف/يفعّل أي عضو من غير ما يحذفه. المدير كمان يشوف ويعدّل **كل** المقالات (مقالاته هو ومقالات أي عضو تاني).
- **عضو (editor)**: يقدر يكتب وينشر مقالات، لكن يشوف ويعدّل **مقالاته هو بس** — مش هيشوف مقالات الأعضاء التانيين، وتبويب "الفريق" مخفي عنه تمامًا.
- النظام ده متطبق فعليًا على مستوى قاعدة البيانات (Row Level Security) مش بس إخفاء في الواجهة، يعني حتى لو حد حاول يتلاعب في المتصفح مش هيقدر يشوف أو يعدّل حاجة مش من صلاحياته.
- لو المدير أوقف عضو (زرار "إيقاف" في صفحة الفريق)، العضو ده مش هيقدر يدخل اللوحة تاني لحد ما المدير يفعّله تاني.
- النظام بيمنع تلقائيًا إزالة صلاحية "مدير" من آخر مدير نشط في النظام، عشان محدش يقفل نفسه برة بالغلط.

## 5) اسم الكاتب وتاريخ النشر (وإظهارهم للجمهور)

- كل مقال بيتسجل عليه اسم كاتبه تلقائيًا وقت الحفظ.
- في محرر المقال، فيه بطاقة "إعدادات الظهور للجمهور" فيها خانتين ✔️:
  - **إظهار اسم الكاتب**: لو اتشالت، اسم الكاتب مش هيظهر في المدونة ولا صفحة المقال (بيفضل محفوظ في قاعدة البيانات بس مخفي عن الزوار).
  - **إظهار تاريخ النشر**: نفس الفكرة بالنسبة للتاريخ.
- ده بيدي مرونة تنشر مقال من غير ما تظهر مين كتبه أو امتى، لو حبيت كده لأي سبب (مثلاً مقال جماعي أو مقال قديم).

## 6) بطاقة "كتب بواسطة" (نبذة عن الكاتب أسفل كل مقال)

- كل عضو (مدير أو عادي) يقدر من تبويب **حسابي** يكتب نبذة قصيرة عن نفسه، يرفع صورة شخصية، ويحدد الاسم اللي عايز يظهر في البطاقة.
- البطاقة بتظهر أوتوماتيك أسفل أي مقال ينشره العضو، بشرط:
  1. يكون سجّل النبذة في حسابه (لو سايبها فاضية، البطاقة مش هتظهر خالص).
  2. خانة "إظهار بطاقة كتب بواسطة" في محرر المقال (تحت "إعدادات الظهور للجمهور") تكون متعلّمة — وهي متعلّمة افتراضيًا.
- البيانات (الاسم، النبذة، الصورة) بتتنسخ على المقال وقت الحفظ أو النشر — يعني لو عدّلت نبذتك بعد كده، التعديل هيظهر في أول مقال تحفظه أو تنشره بعد التعديل، مش في المقالات القديمة أوتوماتيك (لازم تفتحها وتحفظها تاني لو عايز تحدّثها).
- لازم تشغّل تحديث `supabase-schema.sql` مرة كمان عشان الأعمدة والصلاحيات الجديدة الخاصة بالميزة دي تتفعّل.

## 7) جدولة نشر المقالات (النشر التلقائي في معاد محدد)

- في محرر المقال، فيه بطاقة "جدولة النشر" فيها خانة تاريخ ووقت.
- **سيبها فاضية**: زرار "نشر" بينشر المقال فورًا زي العادي.
- **حدد تاريخ ووقت في المستقبل**: زرار النشر بيتحول لـ "جدولة النشر"، وبمجرد ما تضغطه المقال بيتحفظ لكن **يفضل مخفي تمامًا عن الزوار وجوجل** — مش هيظهر في `blog.html` ولا يفتح لو حد جاب رابطه مباشرة — لحد ما يوصل المعاد ده بالثانية، وبعدين يظهر تلقائيًا من غير ما تحتاج تدخل اللوحة تاني أو تضغط أي حاجة.
- الميزة دي شغّالة بمنطق قاعدة البيانات نفسها (مش تطبيق أو سيرفر لازم يفضل شغال)، يعني هتشتغل حتى لو جهازك مقفول وقت المعاد.
- في قائمة "المقالات"، المقال المتجدول بيظهر بعلامة "مجدولة" (لونها مختلف عن "منشور") وجنبها المعاد المحدد، عشان تفرق بسهولة بين اللي منشور فعلاً واللي لسه مستني معاده.
- تقدر تفتح مقال متجدول في أي وقت قبل معاده وتغيّر التاريخ (تقدّمه أو تأخره) أو تمسح خانة التاريخ وتنشره فورًا بدل الجدولة.
- لازم تشغّل تحديث `supabase-schema.sql` مرة كمان عشان الميزة دي تتفعّل (بتتحكم في مين يشوف المقال من قاعدة البيانات نفسها).

## 8) نشر Edge Function (لإضافة أعضاء جدد مباشرة)
ميزة "إضافة عضو" محتاجة صلاحية خاصة سرية (Service Role) مينفعش تتحط في كود الموقع العادي (عشان أي حد يقدر يشوفها). عشان كده عملتها كـ Edge Function منفصلة تتنشر على Supabase نفسه. دلوقتي لما تضيف عضو من اللوحة وتحدد له كلمة مرور وصلاحيته (مدير/عضو)، بيتفعّل حسابه فورًا ويقدر يدخل على طول من غير ما يستنى إيميل — وبعدين يقدر يغيّر كلمة المرور بنفسه من "حسابي" جوه اللوحة. الفنكشن نفسها بتتأكد إن اللي بيطلب الإضافة "مدير" فعلاً قبل ما تنفذ أي حاجة، حتى لو حد حاول يناديها مباشرة من برة اللوحة.

1. نزّل [Supabase CLI](https://supabase.com/docs/guides/cli/getting-started)
2. من مجلد المشروع، شغّل:
```bash
supabase login
supabase link --project-ref uvzcolbxpjsufalgmywz
supabase functions deploy invite-admin
```
3. حط الأسرار (السيكريتس) بتاعة الفنكشن:
```bash
supabase secrets set SUPABASE_URL=https://uvzcolbxpjsufalgmywz.supabase.co
supabase secrets set SUPABASE_ANON_KEY=sb_publishable_rnGIT6p56s9aRCGA0rHG_g_3KS9GUa6
supabase secrets set SUPABASE_SERVICE_ROLE_KEY=xxxxxxxx
```
الـ **Service Role Key** تلاقيه في: **Project Settings → API → service_role key** (سري جدًا، متشاركوش مع حد ولا تحطه في GitHub).

> **ملحوظة:** لو مش عايز تتعامل مع الـ CLI دلوقتي، الموقع والمدونة ولوحة المقالات وتعديل المحتوى هيشتغلوا عادي من غيرها. بس ميزة "إضافة عضو جديد" من داخل اللوحة مش هتشتغل لحد ما تنشر الفنكشن دي. تقدر تضيف أعضاء يدويًا من **Authentication → Add user** في الوقت ده، وبعدين ترفعلهم الـ role من الـ SQL Editor زي الخطوة 3.

> **لو كان عندك نسخة قديمة من الفنكشن منشورة قبل كده**: لازم تعمل `supabase functions deploy invite-admin` تاني بعد التحديث ده عشان تفعّل التحقق من صلاحية "مدير" واختيار الصلاحية للعضو الجديد.

## 9) ارفع الملفات على GitHub / الاستضافة
ارفع كل الملفات دي في نفس المجلد (مستوى واحد):
```
index.html
blog.html
article.html
dashboard.html
style.css
script.js
js/supabase-client.js
```
(ملف `supabase/functions/...` و`supabase-schema.sql` مش لازم يترفعوا مع الموقع نفسه — دول بتستخدمهم بس وقت الإعداد).

## 10) استخدام لوحة التحكم
- افتح `yourdomain.com/dashboard.html`
- سجّل دخول بإيميلك وكلمة المرور
- **المقالات**: محرر مقالات عادي (زي الوورد) — اختار "عنوان H2" لحد "H6" من القائمة، غامق، مائل، قوائم، روابط، وصور جوه المقال (كل صورة بتاخد Alt text بيتسألك عليه وقت الرفع). احفظ كمسودة، انشر فورًا، أو حدد معاد في بطاقة "جدولة النشر" عشان المقال ينشر نفسه تلقائيًا وقتها (تفاصيل في القسم 7). يظهر في `blog.html` بمجرد وصول معاد النشر. صورة الغلاف كمان ليها حقل Alt text مستقل. لو إنت "عضو" (مش مدير) هتشوف مقالاتك إنت بس في القائمة؛ المدير يشوف مقالات كل الأعضاء. وفيه بطاقة "إعدادات الظهور للجمهور" في المحرر للتحكم في ظهور اسم الكاتب وتاريخ النشر.
- **الفريق** (تظهر للمدير فقط): ضيف عضو بإيميله واسمه، اختار صلاحيته (مدير أو عضو)، وحدد له كلمة مرور (فيه زرار "توليد" يعملك واحدة قوية). الحساب بيتفعّل فورًا — ابعتله البيانات بنفسك (واتساب أو إيميل عادي) وهو بعدين يقدر يغيّرها من "حسابي". من نفس الصفحة تقدر تغيّر صلاحية أي عضو، أو توقفه مؤقتًا (يمنعه من الدخول من غير ما تحذف حسابه) — ماعدا نفسك أو آخر مدير نشط في النظام.
- **محتوى الموقع**: تبويب "النصوص" لتعديل كل عناوين وفقرات الصفحة الرئيسية (الاسم، العنوان، كل الأقسام)، وتبويب "الصور" لرفع صورة "نبذة عني" وتحديد Alt text ليها — وهتتحدث في الموقع فورًا.
- **حسابي**: أي عضو (مالك أو عادي) يقدر يغيّر كلمة مروره من هنا في أي وقت، وكمان يكتب نبذة عن نفسه ويرفع صورة شخصية عشان تظهر في بطاقة "كتب بواسطة" أسفل مقالاته (تفاصيل أكتر في القسم 6 فوق).

## ملاحظات أمان
- الـ Publishable Key اللي في `js/supabase-client.js` آمن يتحط في الكود العام (مصمم لكده).
- الـ **Service Role Key** لازم يفضل سري دايمًا — يتحط بس كـ secret في Supabase، وميترفعش على GitHub أبدًا.
